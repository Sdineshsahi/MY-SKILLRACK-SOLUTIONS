TASK:
The program must accept the capacity of three vechicles
and print the capacity/no.of passengers for all combinations of vehicles used.
------------------------------------------------------------------------------
CODE IN C:
-----------
INPUT:
5 4 10
OUTPUT:
4 5 9 10 14 15 19
-----------------------------------------------
CODE IN C:
-----------
#include<stdio.h>
#include<stdlib.h>
void sort(int l,int arr[l]){
    for(int i=0;i<l-1;i++){
        for(int j=i+1;j<l;j++){
            if(arr[i]>arr[j]){
            int t=arr[i];
            arr[i]=arr[j];
            arr[j]=t;
            }
        }
    }
}
int main(){
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
    int arr[3];
    int k=0;
    arr[k++]=a;
    arr[k++]=b;
    arr[k++]=c;
    sort(k,arr);
    int res[10];
    int x=0;
    for(int i=0;i<k;i++){
        res[x++]=arr[i];
        for(int j=i+1;j<k;j++){
            res[x++]=arr[i]+arr[j];
        }
    }
    res[x++]=a+b+c;
    sort(x,res);
    for(int i=0;i<x;i++){
        printf("%d ",res[i]);
    }
}
