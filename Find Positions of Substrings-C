TASK:
-----
The program must accept a string and its SUBSTRINGS as input.
For each substring,the program must print the position of the substring in the main string as the output.
------------------------------------------------------------------------------------------------------
INPUT/OUTPUT:
------------
INPUT:

skillrack
ack
llr
ski

OUTPUT:
3
2
1
----------------------------------
CODE IN C:
-----------------------------
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(){
    char s[101];
    scanf("%s",s);
    int l=strlen(s);
    char k[101];
    while(scanf("%s",k)>0){
        int kl=strlen(k);
        for(int i=0;i<=l-kl;i++){
            int m=1;
            for(int j=0;j<kl;j++){
                if(s[i+j]!=k[j]){
                    m=0;
                    break;
                }
            }
            if(m==1){
                printf("%d\n",i/kl+1);
            }
        }
    }
}
----------------------------------------------------------------------------
